org.gradle.jvmargs=-Xmx2048M -Dfile.encoding=UTF-8 -Dkotlin.daemon.jvm.options\="-Xmx2048M"
org.gradle.caching=true
org.gradle.configuration-cache=false
kotlin.code.style=official

android.useAndroidX=true
android.nonTransitiveRClass=true

# Candidate coordinates only. GitHub Packages is immutable and this agent's
# local credential intentionally has no read:packages scope, so the release
# workflow must re-probe the exact version before an authorized publication.
groupID=com.tencent.kuiklybase
artifactID=datetime
mavenVersion=0.1.0-raft.0
